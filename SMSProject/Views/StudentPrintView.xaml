<UserControl x:Class="SMSProject.Views.StudentPrintView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#FFF7F9FC">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Drukuj studentów"
                   FontSize="18" FontWeight="Bold" Foreground="#FF0047A1"/>

        <!-- Filtry -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="160"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="160"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="160"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <ComboBox Grid.Column="0" x:Name="printGroupFilter"  Height="30" FontSize="13">
                <ComboBoxItem Content="Wszystkie grupy" IsSelected="True"/>
                <ComboBoxItem Content="Grupa 1"/><ComboBoxItem Content="Grupa 2"/>
                <ComboBoxItem Content="Grupa 3"/><ComboBoxItem Content="Grupa 4"/>
            </ComboBox>
            <ComboBox Grid.Column="2" x:Name="printGenderFilter" Height="30" FontSize="13">
                <ComboBoxItem Content="Wszyscy" IsSelected="True"/>
                <ComboBoxItem Content="Mężczyzna"/><ComboBoxItem Content="Kobieta"/>
            </ComboBox>
            <TextBox  Grid.Column="4" x:Name="printSearchBox"    Height="30" FontSize="13" Padding="5,4"
                      ToolTip="Szukaj studenta"/>
            <Button Grid.Column="6" Content="Podgląd" Width="90" Height="30"
                    Background="#FF0047A1" Foreground="White" FontSize="13" BorderThickness="0" Cursor="Hand"
                    Click="previewButton_Click"/>
            <Button Grid.Column="8" Content="Filtruj" Width="90" Height="30"
                    Background="#FFD0D8E8" Foreground="#FF1F2D3D" FontSize="13" BorderThickness="0" Cursor="Hand"
                    Click="filterButton_Click"/>
        </Grid>

        <!-- Podtytuł -->
        <TextBlock Grid.Row="4" Text="Lista studentów do wydruku" FontSize="13" FontWeight="SemiBold"
                   Foreground="#FF0047A1" VerticalAlignment="Top" Margin="0,0,0,6"/>

        <DataGrid x:Name="printStudentsGrid"
                  Grid.Row="4" Margin="0,26,0,0"
                  AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single"
                  GridLinesVisibility="Horizontal" HeadersVisibility="Column"
                  RowBackground="White" AlternatingRowBackground="#FFF0F4FB" FontSize="13">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID"        Binding="{Binding StudentId}"   Width="55"/>
                <DataGridTextColumn Header="Imię"      Binding="{Binding FirstName}"   Width="*"/>
                <DataGridTextColumn Header="Nazwisko"  Binding="{Binding LastName}"    Width="*"/>
                <DataGridTextColumn Header="Nr albumu" Binding="{Binding AlbumNumber}" Width="110"/>
                <DataGridTextColumn Header="Email"     Binding="{Binding Email}"       Width="*"/>
                <DataGridTextColumn Header="Płeć"      Binding="{Binding Gender}"      Width="90"/>
                <DataGridTextColumn Header="Grupa"     Binding="{Binding GroupId}"     Width="70"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Wyczyść" Width="90" Height="32" Margin="0,0,8,0"
                    Background="#FFD0D8E8" Foreground="#FF1F2D3D" FontSize="13" BorderThickness="0" Cursor="Hand"
                    Click="clearPrintButton_Click"/>
            <Button Content="Eksportuj" Width="100" Height="32" Margin="0,0,8,0"
                    Background="#FFD0D8E8" Foreground="#FF1F2D3D" FontSize="13" BorderThickness="0" Cursor="Hand"
                    Click="exportButton_Click"/>
            <Button Content="Drukuj" Width="90" Height="32"
                    Background="#FF0047A1" Foreground="White" FontSize="13" FontWeight="Bold" BorderThickness="0" Cursor="Hand"
                    Click="printButton_Click"/>
        </StackPanel>
    </Grid>
</UserControl>
