<UserControl x:Class="SMSProject.Views.StudentRegistrationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#FFF7F9FC">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="10">

            <!-- Nagłówek -->
            <TextBlock Text="Rejestracja studenta"
                       FontSize="18" FontWeight="Bold"
                       Foreground="#FF0047A1" Margin="0,0,0,16"/>

            <!-- Siatka pól -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="110"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="110"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="10"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="10"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Imię -->
                <TextBlock Grid.Row="0" Grid.Column="0" Text="Imię:" VerticalAlignment="Center" FontSize="14"/>
                <TextBox   Grid.Row="0" Grid.Column="1" x:Name="firstNameTextBox" Height="30" FontSize="14" Padding="5,4"/>

                <!-- Nazwisko -->
                <TextBlock Grid.Row="0" Grid.Column="2" Text="Nazwisko:" VerticalAlignment="Center" FontSize="14" Margin="12,0,0,0"/>
                <TextBox   Grid.Row="0" Grid.Column="3" x:Name="lastNameTextBox" Height="30" FontSize="14" Padding="5,4"/>

                <!-- Nr albumu -->
                <TextBlock Grid.Row="2" Grid.Column="0" Text="Nr albumu:" VerticalAlignment="Center" FontSize="14"/>
                <TextBox   Grid.Row="2" Grid.Column="1" x:Name="albumNumberTextBox" Height="30" FontSize="14" Padding="5,4"/>

                <!-- E-mail -->
                <TextBlock Grid.Row="2" Grid.Column="2" Text="Adres e-mail:" VerticalAlignment="Center" FontSize="14" Margin="12,0,0,0"/>
                <TextBox   Grid.Row="2" Grid.Column="3" x:Name="emailTextBox" Height="30" FontSize="14" Padding="5,4"/>

                <!-- Płeć -->
                <TextBlock Grid.Row="4" Grid.Column="0" Text="Płeć:" VerticalAlignment="Center" FontSize="14"/>
                <ComboBox  Grid.Row="4" Grid.Column="1" x:Name="genderComboBox" Height="30" FontSize="14">
                    <ComboBoxItem Content="Mężczyzna"/>
                    <ComboBoxItem Content="Kobieta"/>
                </ComboBox>

                <!-- Grupa -->
                <TextBlock Grid.Row="4" Grid.Column="2" Text="Grupa:" VerticalAlignment="Center" FontSize="14" Margin="12,0,0,0"/>
                <ComboBox  Grid.Row="4" Grid.Column="3" x:Name="groupComboBox" Height="30" FontSize="14">
                    <ComboBoxItem Content="1" Tag="1"/>
                    <ComboBoxItem Content="2" Tag="2"/>
                    <ComboBoxItem Content="3" Tag="3"/>
                    <ComboBoxItem Content="4" Tag="4"/>
                </ComboBox>
            </Grid>

            <!-- Uwaga -->
            <TextBlock Text="Uzupełnij dane studenta zgodnie z formularzem. Pola wymagane: imię, nazwisko, numer albumu, email, płeć, grupa."
                       FontSize="12" Foreground="Gray" Margin="0,12,0,12" TextWrapping="Wrap"/>

            <!-- Przyciski -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                <Button x:Name="clearStudentButton"    Content="Wyczyść" Width="90" Height="32" Margin="0,0,8,0"
                        Background="#FFD0D8E8" Foreground="#FF1F2D3D" FontSize="13" BorderThickness="0" Cursor="Hand"
                        Click="clearStudentButton_Click"/>
                <Button x:Name="cancelStudentButton"   Content="Anuluj"  Width="90" Height="32" Margin="0,0,8,0"
                        Background="#FFD0D8E8" Foreground="#FF1F2D3D" FontSize="13" BorderThickness="0" Cursor="Hand"
                        Click="cancelStudentButton_Click"/>
                <Button x:Name="saveStudentButton"     Content="Zapisz"  Width="100" Height="32"
                        Background="#FF0047A1" Foreground="White" FontSize="13" FontWeight="Bold" BorderThickness="0" Cursor="Hand"
                        Click="saveStudentButton_Click"/>
            </StackPanel>

        </StackPanel>
    </ScrollViewer>
</UserControl>
