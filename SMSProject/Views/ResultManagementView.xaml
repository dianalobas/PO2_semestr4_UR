<UserControl x:Class="SMSProject.Views.ResultManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#FFF7F9FC">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Zarządzanie wynikami"
                   FontSize="18" FontWeight="Bold" Foreground="#FF0047A1"/>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="160"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="160"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <ComboBox Grid.Column="0" x:Name="filterStudentComboBox" Height="30" FontSize="13"
                      DisplayMemberPath="FullName">
                <ComboBox.Items>
                    <ComboBoxItem Content="Wszyscy studenci" IsSelected="True"/>
                </ComboBox.Items>
            </ComboBox>
            <ComboBox Grid.Column="2" x:Name="filterCourseComboBox" Height="30" FontSize="13"
                      DisplayMemberPath="CourseName">
                <ComboBox.Items>
                    <ComboBoxItem Content="Wszystkie kursy" IsSelected="True"/>
                </ComboBox.Items>
            </ComboBox>
            <ComboBox Grid.Column="4" x:Name="filterGradeComboBox" Height="30" FontSize="13">
                <ComboBoxItem Content="Wszystkie" IsSelected="True"/>
                <ComboBoxItem Content="2.0"/><ComboBoxItem Content="3.0"/>
                <ComboBoxItem Content="3.5"/><ComboBoxItem Content="4.0"/>
                <ComboBoxItem Content="4.5"/><ComboBoxItem Content="5.0"/>
            </ComboBox>
            <Button Grid.Column="6" Content="Szukaj"  Width="90" Height="30"
                    Background="#FF0047A1" Foreground="White" FontSize="13" BorderThickness="0" Cursor="Hand"
                    Click="searchResultButton_Click"/>
            <Button Grid.Column="8" x:Name="refreshResultButton" Content="Odśwież" Width="90" Height="30"
                    Background="#FFD0D8E8" Foreground="#FF1F2D3D" FontSize="13" BorderThickness="0" Cursor="Hand"
                    Click="refreshResultButton_Click"/>
        </Grid>

        <DataGrid Grid.Row="4" x:Name="resultsDataGrid"
                  AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single"
                  GridLinesVisibility="Horizontal" HeadersVisibility="Column"
                  RowBackground="White" AlternatingRowBackground="#FFF0F4FB" FontSize="13">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID"        Binding="{Binding ResultId}"    Width="55"/>
                <DataGridTextColumn Header="Student"   Binding="{Binding StudentName}" Width="*"/>
                <DataGridTextColumn Header="Kurs"      Binding="{Binding CourseName}"  Width="*"/>
                <DataGridTextColumn Header="Ocena"     Binding="{Binding Grade}"       Width="70"/>
                <DataGridTextColumn Header="Data"      Binding="{Binding DateAdded, StringFormat=\{0:dd.MM.yyyy\}}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="editResultButton"   Content="Edytuj" Width="90" Height="32" Margin="0,0,8,0"
                    Background="#FFD0D8E8" Foreground="#FF1F2D3D" FontSize="13" BorderThickness="0" Cursor="Hand"
                    Click="editResultButton_Click"/>
            <Button x:Name="deleteResultButton" Content="Usuń"   Width="90" Height="32"
                    Background="#FFCC0000" Foreground="White" FontSize="13" FontWeight="Bold" BorderThickness="0" Cursor="Hand"
                    Click="deleteResultButton_Click"/>
        </StackPanel>
    </Grid>
</UserControl>
